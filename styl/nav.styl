nav, .navbar
	colorOptions()
	@extend .with-shadow

	width 100%
	display block
	height 64px
	user-select none
	cursor default
	z-index 99

	&.floating
		position fixed
		top 0
		left 0

main
	margin-top 64px
